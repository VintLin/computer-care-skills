interface:
  display_name: "Clean Storage"
  short_description: "Audit macOS, Windows, or Linux disk use safely"
  default_prompt: "Use $clean-storage to audit disk usage and propose safe cleanup options."
