interface:
  display_name: "Manage Paths"
  short_description: "Convert, quote, and open Windows or Mac paths"
  default_prompt: "Use $manage-paths to convert this Windows or Mac path and provide the correct open command when needed."
